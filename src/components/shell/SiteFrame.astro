<div class="site-frame" aria-hidden="true">
	<span class="site-frame__corner site-frame__corner--tl"></span>
	<span class="site-frame__corner site-frame__corner--tr"></span>
	<span class="site-frame__corner site-frame__corner--bl"></span>
	<span class="site-frame__corner site-frame__corner--br"></span>
	<span class="site-frame__index">A7 / RAIN ARCHIVE</span>
</div>

<style>
	.site-frame {
		position: fixed;
		inset: var(--frame-inset);
		z-index: var(--z-overlay);
		border: 1px solid var(--line);
		pointer-events: none;
	}

	.site-frame__corner {
		position: absolute;
		width: 9px;
		height: 9px;
		border-color: var(--ink);
		opacity: 0.58;
	}

	.site-frame__corner--tl {
		top: -1px;
		left: -1px;
		border-top: 1px solid;
		border-left: 1px solid;
	}

	.site-frame__corner--tr {
		top: -1px;
		right: -1px;
		border-top: 1px solid;
		border-right: 1px solid;
	}

	.site-frame__corner--bl {
		bottom: -1px;
		left: -1px;
		border-bottom: 1px solid;
		border-left: 1px solid;
	}

	.site-frame__corner--br {
		right: -1px;
		bottom: -1px;
		border-right: 1px solid;
		border-bottom: 1px solid;
	}

	.site-frame__index {
		position: absolute;
		right: 10px;
		bottom: 8px;
		font-family: var(--font-ui);
		font-size: 9px;
		color: var(--ink-muted);
		letter-spacing: 0;
	}

	@media (max-width: 760px) {
		.site-frame__index {
			display: none;
		}
	}
</style>
